{
  "config": {
    "step": {
      "user": {
        "title": "HomeSide",
        "description": "Connect to HomeSide controller. Leave username and password empty for read-only access.",
        "data": {
          "host": "IP Address",
          "username": "Username (optional)",
          "password": "Password (optional)"
        },
        "data_description": {
          "host": "IP address of the HomeSide controller",
          "username": "Leave empty for read-only access",
          "password": "Leave empty for read-only access"
        }
      }
    }
  }
}
